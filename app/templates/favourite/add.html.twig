{% include 'default/_form.html.twig' with {
    page_title: 'title_add_to_favourite',
    page_title_param: {'%title%': book.title},
    attr: {
        method: 'POST',
        action: url('favourite_add', {'id': book.id}),
    },
    form: form,
    back_to_list_path: 'book_index',
} %}
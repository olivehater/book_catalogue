{% include 'default/_form.html.twig' with {
    page_title: 'title_category_edit',
    page_title_param: {'%title%': category.title},
    attr: {
        method: 'PUT',
        action: url('category_edit', {'id': category.id}),
    },
    form: form,
    back_to_list_path: 'category_index',
} %}
{% include 'default/_form.html.twig' with {
    page_title: 'title_password_change',
    page_title_param: {'%id%': user.id},
    attr: {
        method: 'PUT',
        action: url('user_change_password', {'id': user.id}),
    },
    back_to_user: 'user_show',
} %}